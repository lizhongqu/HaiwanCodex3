<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.haiwancodex.www.mapper.CodeChangeFileMapper">

    <!-- 批量插入文件变更 -->
    <insert id="insertBatch">
        INSERT INTO code_change_file
        (batch_id, file_path, new_code, is_delete_file, created_at)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.batchId}, #{item.filePath}, #{item.newCode}, #{item.isDeleteFile}, NOW())
        </foreach>
    </insert>

    <!-- 根据批次ID删除关联文件 -->
    <delete id="deleteByBatchId">
        DELETE FROM code_change_file WHERE batch_id = #{batchId}
    </delete>

    <!-- 清空工作区所有文件记录 -->
    <delete id="deleteAllByWs">
        DELETE f FROM code_change_file f
        LEFT JOIN code_change_batch b ON f.batch_id = b.id
        WHERE b.workspace_id = #{workspaceId}
    </delete>

    <!-- 查询单批次所有文件 -->
    <select id="selectByBatchId" resultType="com.haiwancodex.www.entity.CodeChangeFile">
        SELECT id, batch_id, file_path, new_code, is_delete_file, created_at
        FROM code_change_file
        WHERE batch_id = #{batchId}
        ORDER BY file_path ASC
    </select>

    <select id="selectById" resultType="com.haiwancodex.www.entity.CodeChangeFile">
        SELECT id, batch_id, file_path, new_code, is_delete_file AS isDeleteFile, created_at AS createdAt
        FROM code_change_file
        WHERE id = #{fileId}
    </select>

</mapper>